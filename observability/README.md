# UDP Observability

UDP v0.2 includes smoke tests and container health checks.

Future observability modules:

- pipeline run table
- row count reconciliation
- schema drift tracking
- freshness checks
- audit logs
- dashboard export
